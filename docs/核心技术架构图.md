# 「小暖」核心技术架构图

> 当前仓库技术架构总览。API = 应用程序编程接口；WS = WebSocket 全双工长连接；ASR = 自动语音识别；TTS = 语音合成；LLM = 大语言模型；DB = 数据库。

## 1. 主架构图

```mermaid
flowchart TB
    subgraph Client["前端客户端"]
        Entry["入口页<br/>web/index.html"]
        Elder["老人端<br/>web/elder<br/>通话 / 回忆 / 角色配置"]
        Parent["子女端<br/>web/parent<br/>今日信号 / 重点事项 / 成本看板"]
        Admin["管理端<br/>web/admin<br/>配置辅助"]

        Entry --> Elder
        Entry --> Parent
        Entry --> Admin
    end

    subgraph ElderRuntime["老人端实时通话运行时"]
        Capture["AudioWorklet 采集<br/>16k mono PCM"]
        Player["PcmPlayer 播放<br/>24k PCM TTS"]
        LocalVAD["本地 VAD 打断<br/>音频门闩丢弃旧轮残尾"]

        Capture --> LocalVAD
        LocalVAD --> Player
    end

    Elder --> Capture
    Player --> Elder

    subgraph Backend["后端 FastAPI"]
        Health["/healthz<br/>凭证与引擎预检"]
        ElderWS["/ws/elder/{elder_id}<br/>老人端 WS 网关"]
        Session["ConversationSession<br/>会话桥接与生命周期"]

        ParentAPI["/api/parent<br/>重点事项 / 信号 / 用量"]
        ElderAPI["/api/elder<br/>老人自己的生活记忆"]
        CharacterAPI["/api/character<br/>声音克隆 + 人格蒸馏"]

        EngineFactory["VoiceEngine 工厂"]
        SeedDuplex["SeeduplexEngine<br/>真实端到端语音"]
        FakeEngine["FakeEngine<br/>无凭证联调"]

        MemoryService["MemoryService<br/>记忆蒸馏 + 上下文注入"]
        SignalService["SignalService<br/>子女端信号生成"]
        CharacterService["CharacterService<br/>角色再生编排"]
        UsageStore["UsageStore<br/>Token 用量记账"]
    end

    subgraph Storage["本地持久化 SQLite<br/>默认 backend/memory.db"]
        KeyFacts[("key_facts<br/>重点事项")]
        LifeMemories[("life_memories<br/>生活记忆")]
        Signals[("signals<br/>子女端结论信号")]
        Characters[("characters<br/>speaker_id + persona_prompt")]
        UsageLog[("usage_log<br/>token 计数")]
    end

    subgraph External["外部云服务"]
        VolcRealtime["火山豆包端到端实时语音<br/>ASR + LLM 对话 + TTS"]
        VolcVoiceClone["火山声音复刻<br/>训练 / 查询 speaker_id"]
        ArkText["火山方舟文本 LLM<br/>记忆蒸馏 / 信号润色 / 人格蒸馏"]
    end

    Elder -->|GET| Health
    Elder -->|WS 二进制 PCM 上行<br/>WS 音频/字幕/状态下行| ElderWS
    Elder -->|GET 自己的回忆| ElderAPI
    Elder -->|HTTP 表单/JSON| CharacterAPI
    Parent -->|HTTP 拉取/配置| ParentAPI

    ElderWS --> Session
    Session --> EngineFactory
    EngineFactory --> SeedDuplex
    EngineFactory --> FakeEngine
    SeedDuplex <-->|StartConnection / StartSession / TaskRequest| VolcRealtime

    Session -->|会话开始读取| MemoryService
    Session -->|会话开始读取 active persona / speaker| CharacterService
    Session -->|会话结束异步触发| MemoryService
    Session -->|会话结束异步触发| SignalService

    MemoryService --> KeyFacts
    MemoryService --> LifeMemories
    MemoryService --> ArkText

    SignalService --> Signals
    SignalService --> ArkText
    SignalService --> UsageStore

    CharacterService --> Characters
    CharacterService --> VolcVoiceClone
    CharacterService --> ArkText
    CharacterService --> UsageStore

    ParentAPI --> KeyFacts
    ParentAPI --> Signals
    ParentAPI --> UsageLog
    ElderAPI --> LifeMemories
    UsageStore --> UsageLog
```

## 2. 核心数据流图

```mermaid
sequenceDiagram
    participant E as 老人端 app.js
    participant G as FastAPI 网关/API
    participant S as ConversationSession
    participant M as MemoryService
    participant SG as SignalService
    participant C as CharacterService
    participant V as 火山实时语音
    participant D as SQLite memory.db
    participant A as 方舟文本 LLM
    participant P as 子女端

    E->>G: GET /healthz 预检语音凭证
    E->>E: 申请麦克风 + AudioWorklet 采集 PCM
    E->>G: 建立 WS /ws/elder/{elder_id}
    G->>S: 创建会话并注入 context/speaker/end 回调

    S->>M: build_context(elder_id)
    M->>D: 读取 key_facts + recent life_memories
    D-->>M: 蒸馏后的结构化记忆
    M-->>S: system_prompt + 空 dialog_context

    S->>C: active_persona / active_speaker
    C->>D: 读取启用角色
    D-->>C: persona_prompt / speaker_id
    C-->>S: 可注入人格与音色

    S->>V: StartSession(system_role, speaker)
    V-->>S: SessionStarted
    S-->>E: {"type":"status","status":"connected"}

    loop 通话中
        E->>S: WS 二进制 PCM 音频帧
        S->>V: TaskRequest 音频流
        V-->>S: ASR 文本 / Chat 文本 / TTS 音频
        S-->>E: 字幕 JSON + TTS 音频帧
        V-->>S: ASR_INFO 用户开口
        S-->>E: {"type":"barge_in"}
        E->>E: 本地停播并关闭音频门闩
    end

    E->>S: hangup 或 WS 断开
    S->>V: FinishSession / Close
    S->>M: 异步 distill(transcript)
    M->>A: 提炼 life_memories + key_facts
    A-->>M: JSON 记忆结果
    M->>D: 写入 key_facts / life_memories

    S->>SG: 异步 generate(transcript)
    SG->>SG: 规则提炼 level / mood / mentions
    SG->>A: 可选 LLM 润色摘要
    A-->>SG: 30 字内结论摘要
    SG->>D: 写入 signals

    P->>G: GET /api/parent/{elder_id}/signals
    G->>D: 读取结论信号
    D-->>P: level / mood / mentions / summary
```

## 3. 模块边界

| 边界 | 当前实现 |
|---|---|
| 实时通话 | 老人端只采集/播放音频，后端持有凭证并代理火山实时语音链路 |
| 记忆注入 | 新会话开始前读取 `key_facts` 与最近 `life_memories`，拼入 `system_prompt` |
| 记忆蒸馏 | 会话结束后异步调用方舟 LLM，失败只跳过本轮，不影响通话 |
| 子女端信号 | 会话结束后生成并落库，子女端通过 HTTP 拉取，不是服务端主动推送 |
| 角色再生 | `speaker_id` 注入 TTS 音色，`persona_prompt` 注入 system prompt 人格 |
| 隐私硬边界 | 原始对话只存在会话内存中；DB 只保存结构化记忆、结论信号和 token 计数 |

## 4. 当前架构的关键事实

1. 发声主链路只走 `VOLC_*` 对应的火山 openspeech 实时语音；`ARK_API_KEY` 不参与发声。
2. 方舟文本 LLM 只负责三类离线/异步任务：记忆蒸馏、信号摘要润色、人格蒸馏。
3. `ConversationSession` 是核心解耦点：它只依赖 `VoiceEngine` 抽象，不关心底层是真实 Seeduplex 还是 FakeEngine。
4. 子女端只能看到 `signals` 这类结论数据，看不到 `life_memories` 与原始对话。
5. `backend/memory.db` 是当前单机 MVP 的状态中心；后续多用户/多端并发扩展时，它是最明确的替换点。
